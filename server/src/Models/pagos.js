const {Schema,model} = require('mongoose');

new Schema({
    departamento:{type:int,require:true},
    monto:{type:doble,require:true},
    departamento:{type:int,require:true},
    departamento:{type:int,require:true},
    departamento:{type:int,require:true},
});